https://blog.csdn.net/qq_41176306/article/details/124650401

diff ssr 长列表渲染

vue3.2新指令v-memo，相当于可以设置条件的v-once

路由懒加载
keep-alive缓存组件提高性能，防止重复创建组件
v-show防止重复创建爱你
v-for v-if不再一起，提高性能
v-once和v-memo
事件销毁
按需引入
图片懒加载
子组件分割策略：大组件分为多个小组件，但注意不要过度分割，尤其是为了所谓的组件抽离将一些不需要渲染的组件特意抽出来，实际上组件的性能小号远大于DOM
服务端渲染SSR 静态网站生成SSG

vue3编译器优化

vuex module namespace

提交规范；husky  lint-staged
ex-lint

后台管理系统：管理公众号，低代码活动页
el-admin-template  el-element-admin

ajax：
web socket

uniapp：
uni.reLoad()
getCurrentPages()
小程序分包预下载
路由url可传递多个参数
路由跳转url太长不行，需要encodeURL和decodeURL处理参数后在传递
image懒加载
image :src 绑定本地路径正常
click tap区别
uniapp网络请求
navbar隐藏
uniapp各平台样式相同
设计稿
微信和uniapp的vhvw视口大小 滚动click100ms
tabbar路由自动keepalive   
uni对象怎么在template使用
uni-ui  可在源代码修改样式
uniapp没有闭包
$http传参，exclude拦截器
static的文件不会被编译（不能放css、js等），专门用来放静态资源（图片、音视频等）

其他：
牛客网，公众号，浏览器兼容
